- GitLab CI/CD - показать как выглядит в реальности
  - Артефакты в GitLab CI/CD
  - Пример показать и разобрать (скриншоты)
  - Что такое stage, preprod, prod (тестовые стенды)
- systemd - где конфигурация сервисов и пример базовый https://habr.com/ru/companies/southbridge/articles/255845/
- Зомби процессы что такое и пример как получаются они
- Про kill какие сигналы есть
  - SIGINT - самый безобидный сигнал завершения, означает Interrupt. Он отправляется процессу, запущенному из терминала с помощью сочетания клавиш Ctrl+C. Процесс правильно завершает все свои действия и возвращает управление;
  - SIGTERM - немедленно завершает процесс, но обрабатывается программой, поэтому позволяет ей завершить дочерние процессы и освободить все ресурсы;
  - SIGKILL - тоже немедленно завершает процесс, но, в отличие от предыдущего варианта, он не передается самому процессу, а обрабатывается ядром. Поэтому ресурсы и дочерние процессы остаются запущенными.
- Показать `netstat -tulpn` как выглядит
- iptables - что такое, как используется, с примерами https://losst.pro/nastrojka-iptables-dlya-chajnikov
- пример установки SSH `sudo apt install openssh-server` и `sudo systemctl enable sshd`
  - ssh конфиг и отключение входа по паролю в файле `/etc/ssh/sshd_config` раскоментировать `#PasswordAuthentication no` и перезапустить ssh сервис
  - Что находится в папке `.ssh`
- Системные вызовы какие бывают и что это такое
- Что такое трассировка приложений на python
- Пример sed как заменить слово в файле
  - `sed 's/ПЕРВОЕ_СЛОВО/ВТОРОЕ_СЛОВО/g' /ПУТЬ/К/ФАЙЛ` - `s` заменить, `/` разделитель, `g` глобально, то есть не только первое
- Повторить чем контейнеризация лучше виртуализации
- Вольюмы повторить в docker, зачем нужны и как выглядят показать
- pull или rebase? Я использовал pull в IDE
- Что кроме docker images можно хранить в nexus
- Что такое нагрузочное тестирование и как его делают
- Показать Grafana и Datasource на примере (можно мой Keycloak)
  - Ты должен был понять что он неправ (насчет коннекторов в Grafana) и рассказать нормально как работает Grafana + Prometheus + exporters
- crontab в линукс, какие команды
- понимание подхода «Infrastructure as Code» в целом
